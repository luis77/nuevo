<div class="row"><!--atributos de bootstrap. todos se pueden buscar en getbootstrap.com esta la documentacion-->
  <div class="col-md-3 center-block no-float"><!--atributos de bootrstrap. el center block centra pero como el col-md tiene un float left impide centrarlo, por eso se creo la clase no-float y se definio en el scss-->
    
          <h2><span class="glyphicon glyphicon-user"></span>Iniciar sesión</h2>

      <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: {role: "form"}) do |f| %>
      <!--html: {role: "form"}) sirve para agregar atributos a la etiquete form, tenemos que especificar un hash con todas las etiquetas html q queremos agregar. con esto el generador sabe q tiene q agregar este rol a la etiqueta del formulario-->

        <div class="field form-group"><!--los formularios de bootstrap cada grupo de controles tiene una clase form-group-->
          <%= f.label :email %><br />
          <%= f.email_field :email, autofocus: true, class: "form-control" %><!--form-control es la clase de bootstrap q tienen los controles -->
        </div>

        <div class="field form-group"><!--los formularios de bootstrap cada grupo de controles tiene una clase form-group-->

          <%= f.label :password %><br />
          <%= f.password_field :password, autocomplete: "off", class: "form-control" %><!--form-control es la clase de bootstrap q tienen los controles -->
        </div>

        <% if devise_mapping.rememberable? -%>
          <div class="field">
            <%= f.check_box :remember_me %>
            <%= f.label :remember_me %>
          </div>
        <% end -%>

        <div class="actions, text-center"><!--text center es una clase de bootstrap-->
          <%= f.submit "Log in", class: "btn btn-info" %><!-- class: "btn btn-info" es un boton azul de bootstrap-->
        </div>
      <% end %>
      <div class="text-center"><!--text center es una clase de bootstrap-->
        <%= render "devise/shared/links" %>
      </div>
      

  </div>  
</div>
